import React from 'react';

const Home = () => (
  <div>
    <div>I'm the Best ASD home component</div>
    <button onClick={() => console.log('Pressed')}>
      Press Me!
    </button>
  </div>
);

export default Home;
